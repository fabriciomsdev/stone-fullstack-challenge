version: "3.7"
services:
  rest_api:
    restart: always
    build: "./backend/rest-api/"
    container_name: rest-api-service
    command: /bin/sh -c 'sudo gunicorn -b 0.0.0.0:8082 service.start:app'
    volumes:
      - ./backend/rest-api/:/code/app/
    ports:
      - 8082:8082
    env_file:
      - ./backend/rest-api/.env
    depends_on:
      - database
    links:
      - database:database

  database:
    build: "./backend/database/"
    ports:
      - '3307:3306'
    command: mysqld --default-authentication-plugin=mysql_native_password
    container_name: mysql-container
    environment:
       MYSQL_DATABASE: 'logistic'
       MYSQL_USER: 'stone'
       MYSQL_PASSWORD: '77991717025'
       MYSQL_ROOT_PASSWORD: '77991717025'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./backend/database/data:/var/lib/mysql
    restart: always